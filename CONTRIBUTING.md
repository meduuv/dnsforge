# Contributing

Use Python 3.10 or newer and keep changes focused. Run:

```text
python -m unittest discover -s tests
```

Resolver behavior should remain read-only and suitable for authorized diagnostics.
